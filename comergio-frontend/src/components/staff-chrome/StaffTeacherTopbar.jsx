import InstitutionalPortalHeader from '../InstitutionalPortalHeader';

/** Thin alias: teacher-style staff topbar shared across staff portals. */
export default function StaffTeacherTopbar(props) {
  return <InstitutionalPortalHeader {...props} />;
}
